name: Test
on:
    push:
        branches:
            - master
    create:
jobs:
    test:
        name: Test
        runs-on: ${{ matrix.os }}
        strategy:
            matrix:
                os: 
                    - windows-latest
                    - ubuntu-latest
        steps:
            - 
                name: Get the sources
                uses: actions/checkout@v2
            -
                name: Create a release draft
                uses: "./"
                id: create-release-draft
